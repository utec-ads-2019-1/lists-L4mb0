//
// Created by root on 4/11/19.
//

#ifndef SORT_ALGORITHMS_L4MB0_INSCLUDESS_H
#define SORT_ALGORITHMS_L4MB0_INSCLUDESS_H

#include <iostream>
#include <thread>
#include <string>
#include <vector>
#include <map>
#include <set>
#include <limits>
#include <queue>
#include <cmath>
#include <algorithm>
#include <bits/stdc++.h>
#include <future>
#define ll long long int
#define scl1(a)             scanf("%lld", &a)
#define REP(i,n) FOR(i,0,n)
#define pb(x) push_back(x)
#define fst first
#define snd second
#define foritpnt for(auto it=datos->begin(); it != datos->end(); it++)
#define forit for(auto it=datos.begin(); it != datos.end(); it++)
#define sz(x) int(x.size())
#define all(v) v.begin(),v.end()
#define mp(x,y) make_pair(x,y)
#define fastcin std::ios::sync_with_stdio(false); std::cin.tie(nullptr);

#endif //SORT_ALGORITHMS_L4MB0_INSCLUDESS_H
